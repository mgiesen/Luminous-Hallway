// Server address for API calls and WebSocket
const serverAddress = 'localhost:80';

// Full HTTP base URL for fetch requests
const apiBaseUrl = `http://${serverAddress}`;

// WebSocket URL
const wsUrl = `ws://${serverAddress}`;
